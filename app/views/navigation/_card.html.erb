<%# locals: (options:, content:) %>
<div class="card <%= options[:class] %> bg-base-100 shadow-sm">
  <div class="card-body">
    <% if options[:title].present? %>
        <h3 class="card-title"><%= options[:title] %></h3>
    <% end %>

    <div>
      <%= content %>
    </div>
  </div>
</div>
